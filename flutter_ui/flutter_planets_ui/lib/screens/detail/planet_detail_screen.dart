import 'package:flutter/material.dart';

class PlanetDetailScreen extends StatelessWidget {
  PlanetDetailScreen(String id);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
